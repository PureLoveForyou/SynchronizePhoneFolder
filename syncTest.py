# from dirsync import sync

# source = './source'
# dst = './dst'
# sync(sourcedir=source, targetdir=dst, action='diff')
# sync(sourcedir=source, targetdir=dst, action='sync')
# # sync(sourcedir=source, targetdir=dst, action='update')

